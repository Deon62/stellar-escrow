[package]
name = "lumenswags"
version = "0.1.0"
description = "Lumenswags escrow: buyer funds, seller ships + evidence, buyer confirms, seller receives"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
